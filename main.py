import robo_pack.robotrobotan = robo_pack.robot.Robot('Roboko')robotan.say_hello()user_name = input()restaurant_name = robotan.reading_csv()#店名、カウントをリスト化# print(restaurant_name)店名がしっかり入っていることをチェックrestaurant_name = sorted(restaurant_name,key=lambda x: x['Count'], reverse=True)# print(restaurant_name)robotan.check_restaurant(restaurant_name,user_name)print('処理は終了しました')# robotan.add_restaurant(user_name)# robotan.good_bye(user_name)# robotan.writing_csv(favorite_restaurant)# with open(restaurant_file, 'r') as csv_file:#     reader = csv.DictReader(csv_file)#     for row in reader:#         if row['Name'] != favorite_restaurant:#             with open('restaurant.csv', 'a') as csv_file:#                 writer = csv.DictWriter(csv_file, fieldnames = fieldnames)#                 writer.writeheader()#                 writer.writerow({'Name': favorite_restaurant, 'Count': 1})#             break#         else:#             with open(restaurant_file, 'a') as write_csv_file:#                 if row['Name'] == favorite_restaurant:#                     name_count = row['Count']#                     csv.DictWriter(write_csv_file, fieldnames=fieldnames).writerow({'Name': favorite_restaurant, 'Count': int(name_count) + 1})            # ここにcsvに書き込む記述を書く。# csvを書き込む記述OK# print(user_name, 'さん', 'ありがとうございました。')# print('良い一日を！さようなら。')